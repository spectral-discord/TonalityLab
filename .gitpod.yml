tasks:
  - name: Terminal
    openMode: split-left
    before: cd /workspace/TonalHub/TonalHub
  - name: "Dev Servers"
    openMode: split-right
    before: |
      cd /workspace/TonalHub/TonalHub
    init: yarn install
    command: yarn rw dev --fwd="--allowed-hosts localhost --allowed-hosts $(gp url 8910 | sed -e "s/https:\/\///g") --client-web-socket-url=$(gp url 8910 | sed -e "s/https/wss/g")/ws"

ports:
  - port: 8910
    onOpen: open-browser
    visibility: public
  - port: 8911
    onOpen: ignore
    visibility: public